# 앞으로 반영할 것

확정된 요구사항을 잊지 않도록 적어 둡니다. **아직 코드에는 반영하지 않았습니다.**
반영할 때는 이 문서의 수치를 그대로 옮기고, 반영이 끝난 항목은 여기서 지웁니다.

## 1. 카드팩별 확률 개편 (구조는 반영됨, 수치는 테스트 중)

> 팩을 **일반팩 · 프리미엄팩** 두 종류로 나누고 팩마다 확률표를 갖도록 구조는 이미
> 반영했습니다(`lib/gacha.ts`의 `PACK_RATES`). 지금 들어간 값은 **테스트용**이라
> 고급 카드가 잘 나오게 맞춰져 있습니다. 아래 표가 최종 확정 수치입니다.


팩마다 등장 등급과 확률이 다릅니다.

| 팩 | 등장 등급 | 지정된 확률 |
| --- | --- | --- |
| 일반 카드팩 | 일반 · 실버 · 골드 · 레전드 | 골드 **0.1%**, 레전드 **0.01%** |
| 이벤트 팩 | 일반 · 실버 · 골드 · 라이브 · 레전드 | 골드 **0.2%**, 라이브 **0.5%**, 레전드 **0.02%** |

- 나머지 확률은 일반 · 실버가 나눠 가집니다(배분 비율은 아직 미정 — 정해지면 여기 적습니다).
- **등급 사다리가 지금 코드와 다릅니다.** 현재는 `일반 · 실버 · 골드 · 라이브 · 월드`이고,
  위 표에서는 **레전드가 최상위**입니다. 반영할 때 `World` → `레전드`로 정리하고,
  라이브는 이벤트 팩 전용 등급으로 둘지 결정해야 합니다.
- 지금 구조에서 바꿀 곳: `lib/rarity.ts`의 `RARITY_WEIGHTS`가 전역 확률 하나뿐입니다.
  팩마다 다른 확률표를 주려면 `PackDef`에 `rates` 필드를 두고 `drawSession`이 그것을
  사용하도록 바꾸면 됩니다. 천장·픽업 로직은 그대로 재사용할 수 있습니다.
- 0.01% 같은 낮은 확률은 천장(현재 30회)과 함께 다시 계산해야 체감이 맞습니다.

## 2. 실시간 경기 진행과 경기 개입

- 라운드마다 **현실 시간대**를 배정합니다. 예: 1라운드 10:00 ~ 10:30.
- 경기가 진행되는 동안 **개입**할 수 있습니다(전술 변경, 교체 등).
- **특정 시간에 개입하면 골드를 추가로 지급**합니다.

설계 시 고려할 점:

- 경기 계산은 이미 틱 단위 엔진(`lib/matchEngine.ts`)이라 구간 진행이 가능합니다.
  현실 시간에 맞추려면 "지금이 몇 분인지"를 시각에서 계산해 그 분까지 틱을 돌리면 됩니다.
- 경기 시간이 현실 시간에 묶이면 진행 상태를 **서버 시간 기준**으로 판정해야 합니다.
  지금은 세이브가 브라우저 `localStorage`에만 있어서 기기 시간을 바꾸면 조작이 가능합니다.
  실시간 일정과 보상이 들어가는 시점에는 서버(또는 최소한 시간 API)가 필요합니다.
- 알림(경기 시작 시각 안내)과 결장/자동 진행(접속하지 않았을 때) 규칙도 함께 정해야 합니다.
- **경기 중 개입은 이미 반영되었습니다.** 교체·전술 지시를 언제든 내리고 파울 · 골 · 아웃 ·
  하프타임에 적용하는 방식이며(`components/tabs/MatchTab.tsx`), 남은 것은 *현실 시간 배정*과
  *특정 시각 개입 보상*입니다.

## 3. 데일리 미니게임 (실시간 리그 전환 이후)

리그 경기가 현실 시간표로 옮겨가면, 지금처럼 **아무 때나 바로 시작하는 경기**는
리그에서 빠지고 **데일리 미니게임**으로 남깁니다.

- **하루 10판** 제한. 자정(세이브 기준 날짜)에 초기화합니다.
- 리그 순위·승격·강등에는 영향을 주지 않고, 골드와 선수 경험치만 줍니다.
- 지금 있는 즉시 경기 화면(관전/텍스트 모드, 경기 중 개입)을 그대로 재사용하면 됩니다.
- 구현 지점: `lib/daily.ts`의 일일 카운터에 `miniGamesPlayed`를 추가하고,
  `lib/schedule.ts`에서 리그·컵 일정과 미니게임을 분리합니다. 보상은 `matchReward()`를
  낮춘 별도 표를 씁니다.

## 4. 회원가입 · 로그인 · 게시판 (서버 필요)

지금 게임은 **브라우저 `localStorage`에만** 저장되는 완전한 클라이언트 앱이라,
계정과 게시판은 **서버와 데이터베이스가 있어야** 만들 수 있습니다. 정해야 할 것:

- **어떤 서비스를 쓸지**: Supabase(가장 간단, 인증+DB+게시판 테이블 한 번에) /
  Vercel Postgres + Auth.js / Firebase 중 하나. 계정 생성과 키 발급은 프로젝트 소유자만
  할 수 있습니다(`NEXT_PUBLIC_SUPABASE_URL` 같은 환경 변수를 Vercel에 등록).
- **세이브 이전**: 로그인하면 지금 브라우저에 있는 세이브를 계정으로 올리고, 이후에는
  서버를 정본으로 씁니다. 로그인 전 진행 상황을 잃지 않도록 1회 병합 절차가 필요합니다.
- **부정 방지**: 세이브가 서버로 가면 골드·카드 수치를 클라이언트가 그대로 보내면 안 되고,
  뽑기·경기 결과 계산이 서버로 옮겨가야 합니다(= 적지 않은 작업량).
- **게시판**: 글·댓글 테이블, 작성자 검증, 신고/삭제, 욕설 필터 정도가 최소 범위입니다.
