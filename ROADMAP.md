# 앞으로 반영할 것

확정된 요구사항을 잊지 않도록 적어 둡니다. **아직 코드에는 반영하지 않았습니다.**
반영할 때는 이 문서의 수치를 그대로 옮기고, 반영이 끝난 항목은 여기서 지웁니다.

## 1. 카드팩별 확률 개편 (구조는 반영됨, 수치는 테스트 중)

> 팩을 **일반팩 · 프리미엄팩** 두 종류로 나누고 팩마다 확률표를 갖도록 구조는 이미
> 반영했습니다(`lib/gacha.ts`의 `PACK_RATES`). 지금 들어간 값은 **테스트용**이라
> 고급 카드가 잘 나오게 맞춰져 있습니다. 아래 표가 최종 확정 수치입니다.


팩마다 등장 등급과 확률이 다릅니다.

| 팩 | 등장 등급 | 지정된 확률 |
| --- | --- | --- |
| 일반 카드팩 | 일반 · 실버 · 골드 · 레전드 | 골드 **0.1%**, 레전드 **0.01%** |
| 이벤트 팩 | 일반 · 실버 · 골드 · 라이브 · 레전드 | 골드 **0.2%**, 라이브 **0.5%**, 레전드 **0.02%** |

- 나머지 확률은 일반 · 실버가 나눠 가집니다(배분 비율은 아직 미정 — 정해지면 여기 적습니다).
- **등급 사다리가 지금 코드와 다릅니다.** 현재는 `일반 · 실버 · 골드 · 라이브 · 월드`이고,
  위 표에서는 **레전드가 최상위**입니다. 반영할 때 `World` → `레전드`로 정리하고,
  라이브는 이벤트 팩 전용 등급으로 둘지 결정해야 합니다.
- 지금 구조에서 바꿀 곳: `lib/rarity.ts`의 `RARITY_WEIGHTS`가 전역 확률 하나뿐입니다.
  팩마다 다른 확률표를 주려면 `PackDef`에 `rates` 필드를 두고 `drawSession`이 그것을
  사용하도록 바꾸면 됩니다. 천장·픽업 로직은 그대로 재사용할 수 있습니다.
- 0.01% 같은 낮은 확률은 천장(현재 30회)과 함께 다시 계산해야 체감이 맞습니다.

## 2. 실시간 경기 진행과 경기 개입

- 라운드마다 **현실 시간대**를 배정합니다. 예: 1라운드 10:00 ~ 10:30.
- 경기가 진행되는 동안 **개입**할 수 있습니다(전술 변경, 교체 등).
- **특정 시간에 개입하면 골드를 추가로 지급**합니다.

설계 시 고려할 점:

- 경기 계산은 이미 틱 단위 엔진(`lib/matchEngine.ts`)이라 구간 진행이 가능합니다.
  현실 시간에 맞추려면 "지금이 몇 분인지"를 시각에서 계산해 그 분까지 틱을 돌리면 됩니다.
- 경기 시간이 현실 시간에 묶이면 진행 상태를 **서버 시간 기준**으로 판정해야 합니다.
  지금은 세이브가 브라우저 `localStorage`에만 있어서 기기 시간을 바꾸면 조작이 가능합니다.
  실시간 일정과 보상이 들어가는 시점에는 서버(또는 최소한 시간 API)가 필요합니다.
- 알림(경기 시작 시각 안내)과 결장/자동 진행(접속하지 않았을 때) 규칙도 함께 정해야 합니다.
- **경기 중 개입은 이미 반영되었습니다.** 교체·전술 지시를 언제든 내리고 파울 · 골 · 아웃 ·
  하프타임에 적용하는 방식이며(`components/tabs/MatchTab.tsx`), 남은 것은 *현실 시간 배정*과
  *특정 시각 개입 보상*입니다.

## 3. 회원가입 · 로그인 · 게시판 — 반영 완료

Supabase로 반영했습니다(README의 "계정과 게시판 켜기" 참고). 남은 과제만 적어 둡니다:

- **어떤 서비스를 쓸지**: Supabase(가장 간단, 인증+DB+게시판 테이블 한 번에) /
  Vercel Postgres + Auth.js / Firebase 중 하나. 계정 생성과 키 발급은 프로젝트 소유자만
  할 수 있습니다(`NEXT_PUBLIC_SUPABASE_URL` 같은 환경 변수를 Vercel에 등록).
- **세이브 이전**: 로그인하면 지금 브라우저에 있는 세이브를 계정으로 올리고, 이후에는
  서버를 정본으로 씁니다. 로그인 전 진행 상황을 잃지 않도록 1회 병합 절차가 필요합니다.
- **부정 방지**: 세이브가 서버로 가면 골드·카드 수치를 클라이언트가 그대로 보내면 안 되고,
  뽑기·경기 결과 계산이 서버로 옮겨가야 합니다(= 적지 않은 작업량).
- **게시판**: 글·댓글 테이블, 작성자 검증, 신고/삭제, 욕설 필터 정도가 최소 범위입니다.

## 4. 핸드오프 문서 검토 결과 (2026-09-03)

`docs/football-day-claude-code-handoff.txt`를 코드와 대조한 결과입니다. 그 문서는
그린필드를 전제로 쓰여 있어서, **그대로 실행하면 이미 만든 것에 다시 Gap Analysis를
쓰게 됩니다.** 아래를 먼저 읽고 착수하세요.

### 이미 구현된 것 — 문서가 "제안"하는 것 중 상당수

| 문서 항목 | 실제 |
| --- | --- |
| §3.4 연속값 전술 입력 | `lib/tactics/params.ts` — 21개 파라미터. 문서 목록과 거의 1:1 |
| §3.4 점유/비점유/전환 분리 | `lib/tactics/phases.ts` — 국면별 파라미터 |
| §4 Headless 엔진 | `lib/matchEngine.ts`, `lib/tactics/sequence.ts`. rng 주입식이라 UI 없이 돕니다 |
| §8 통계 테스트 | `tests/tacticsSim.test.ts` — 180경기 표본 |
| §9 관측 지표 | `lib/tactics/metrics.ts` — PPDA, 패스 성공률, 점유율. `report.ts` |
| §2.3 팀컬러 | `lib/teamColor.ts`, 특성은 `lib/traits.ts` |
| §2.4 승강제 리그 | `lib/league.ts` — 20팀, 3/1/0점, 5부, 승격 2위·강등 18위. 컵은 `lib/cup.ts` |
| §5 자동 교체 | `lib/autoSub.ts` — 체력·부상 조건, 5명 제한 |
| §5 경기 중 개입 | `components/useLiveEngine.ts`, `components/tabs/MatchTab.tsx` |

설계 문서도 `docs/TACTICS_ENGINE_DESIGN.md`, `docs/TACTICS_DATA_SOURCES.md`에 이미
있습니다.

### 문서가 맞게 짚은 진짜 갭

1. **seed와 engineVersion을 기록하지 않습니다.** 엔진은 `rng`를 주입받게 잘 만들어져
   있는데 기본값이 `Math.random`이고, `MatchResult`(`lib/types.ts`)에 seed도
   engineVersion도 없습니다. **지금은 이미 치른 경기를 재현할 수 없습니다.** 밸런스
   비교·버그 조사의 전제라서 이게 1순위입니다. 고치는 비용은 작습니다.
2. **서버 스케줄 리그가 없습니다.** 리그는 전부 클라이언트에서 돌고 플레이어가 버튼을
   눌러야 진행됩니다. 문서 §2.4·§5의 "정해진 시간에 자동 진행"은 0% 구현입니다.
   위 2항과 같은 이야기입니다.
3. PvP 없음.
4. PlayerIdentity/PlayerCard 분리 없음. 지금은 `PlayerDef` + `Card`라 시즌·버전 개념이
   없습니다.

### 문서와 현재 아키텍처의 충돌 — 착수 전에 정리할 것

문서는 **서버 권위 + DB + WebSocket/SSE**를 당연한 전제로 깔고 있습니다(§2.4 스케줄러,
§4.3 스냅샷 보존, §5 "서버가 모든 명령의 유효성과 적용 시점을 결정", §8 부하 테스트).

현재는 정반대입니다. 진실은 브라우저 `localStorage`에 있고 서버는 인증·클라우드
세이브·게시판·뽑기 확정만 합니다. 문서대로 가려면 **경기 시뮬레이션 자체를 서버로**
옮겨야 하고, 그러면 선수 명단·전술 엔진·성장이 전부 서버에도 있어야 합니다. 뽑기
하나 옮기는 데 이미 354KB 번들이 나갔습니다.

즉 문서가 "설계 원칙"처럼 적어둔 것들이 실제로는 **가장 비싼 결정**입니다. 게다가
`docs/SECURITY_ARCHITECTURE.md`에 단계적 서버 이전 계획이 따로 있어서 두 문서가
서로를 모릅니다. **먼저 정리할 것은 이 둘의 관계**지 Gap Analysis가 아닙니다.

비용 감각: 유저 100명 × 하루 10경기면 Edge Function 호출이 월 3만 건입니다. 무료 티어
안이지만 1,000명이면 30만 건이고 경기당 CPU 시간이 붙습니다. 착수 전에 계산하세요.

### 문서의 오류 또는 결정이 필요한 곳

- **§6 "과금 아이템이 경기 중 직접적인 능력치 우위를 구매하게 하지 않는다" — 지금
  상점이 정확히 그걸 팔고 있습니다.** 완전 회복제·치료 키트는 컨디션과 부상을
  되돌리는데, 컨디션은 `conditionFactor`로 능력에 직접 곱해집니다. 원칙을 지킬지,
  아이템을 접근성 장치로 인정하고 문서를 고칠지 결정해야 합니다. (검토 의견: 후자.
  체력 회복은 시간이 해결해줄 것을 앞당기는 것이라 능력치 상한을 사는 것과 다릅니다.)
- **§0 출처 신뢰도가 균일하지 않습니다.** 공식 보도자료·전자신문·게임메카는 괜찮지만
  개인 블로그 3건이 섞여 있습니다. **밸런스 수치의 근거로 쓰지 마세요.**
  `docs/TACTICS_DATA_SOURCES.md`와 어느 쪽이 우선인지도 정해야 합니다.
- **§0 라이선스 항목이 이미 내린 결정을 반영하지 않습니다.** "실제 선수·클럽 명칭은
  별도 라이선스 검토 필요"라고 열어뒀는데, 이 프로젝트는 이미 **전부 가상 이름**으로
  갔습니다(선수 2,720명, 클럽 160팀).
- **§11 실행 프롬프트는 최초 1회용입니다.** "코드를 수정하지 마"로 끝나는데, 저장소에
  들어간 문서에 그대로 남아 있어서 **나중에 읽는 세션이 상시 규칙으로 오해**할 수
  있습니다. 분석은 이 절로 끝났습니다.

## 5. 미결 항목 (우선순위 — 2026-09-03 재조정)

멀티플레이 핵심 루프(동일 리그 실유저 매칭·실시간 진행·PvP)를 확률·경제 수치보다
먼저 가져가기로 결정했습니다. 확률/조각 경제는 테스트 기간이 끝난 뒤 실데이터로
조정합니다. 아래가 새 순서이고, 이전 순서(seed → 확률확정 → 조각경제 → 서버화)는
이걸로 대체합니다.

1. ~~**seed + engineVersion 기록**~~ — **반영 완료 (2026-09-03).** `MatchResult`에
   `seed`/`engineVersion`이 필수 필드로 붙었고(`lib/types.ts`), `lib/match.ts`의
   `simulateMatch`는 seed가 없으면 새로 만들어 그 seed로 rng를 파생시킵니다 — 같은
   seed + `ENGINE_VERSION`(`lib/matchEngine.ts`)이면 스코어·득점자·이벤트가
   그대로 재현됩니다(`tests/match.test.ts`의 "match reproducibility"). 다만
   실시간 관전 모드(`useLiveEngine`가 `advance()`를 시드 없는 rng로 직접 돌림)는
   seed를 기록만 하고 아직 재현은 못 합니다 — 그건 다음 항목(경기 판정 서버화)의
   몫입니다. `MatchSummary`(세이브 history)는 두 필드가 선택값이라 기존 세이브도
   그대로 읽힙니다.
2. ~~**경기 판정 서버화**~~ — **반영 완료 (2026-09-03).** 리그·컵 경기가
   `supabase/functions/simulate-match`를 통해 서버에서 판정됩니다. 클라이언트는
   스쿼드·전술·상대 정보를 제출하고, 서버가 saves에서 직접 읽은 카드로
   `evaluateSquad`를 다시 계산해 소유하지 않은 카드·부상 선수·11명 미달·레벨
   상한 초과를 거부합니다(클라이언트가 보낸 rating은 신뢰하지 않음). division도
   클라이언트가 아니라 저장된 세이브에서 읽습니다. 보상은 `commit_match`
   RPC(service_role 전용)가 `match_results`에 기록하고 같은 트랜잭션에서
   `gold_ledger`에 반영합니다.
   - **범위 밖으로 남긴 것**: 상대 이름·평점은 아직 서버가 리그 대진을 갖고
     있지 않아 클라이언트가 알려준 값을 그대로 씁니다(보상은 division·승패로만
     정해지므로 상대를 부풀려도 보상은 안 늘어남 — 3항에서 실제 대진으로
     교체됩니다). **경기 중 실시간 개입(전술 변경·교체)은 잠시 껐습니다** —
     클라이언트는 서버가 이미 확정한 결과를 같은 seed로 재생만 하고
     (`useLiveEngine`에 seed 주입), 재생 도중 지시는 무시되고 안내 문구만
     뜹니다. 개입을 서버가 접수하는 건 4항의 일입니다. 친선 경기(미니게임)는
     이번 범위에 넣지 않았습니다 — 순위에 영향이 없어 우선순위가 낮았습니다.
3. **동일 리그 실제유저 매칭 v1** — 20팀 유지 + 유저 12명 목표 + 나머지 AI 8팀
   보충. 서버가 리그 배정·일정·스쿼드 스냅샷 검증·결과·순위표·보상 원장을 갖고,
   클라이언트는 전술/스쿼드를 제출하고 같은 seed로 재생만 합니다. 부정 방지:
   서버 시계만 사용, 경기 전 스쿼드 스냅샷 잠금, 소유 카드/부상/11명/레벨캡 검증,
   fixture별 reward 멱등키, 운영자/테스트 계정 공개 리그 배제.
   **설계 완료 (2026-09-03), 구현 전.** `docs/LEAGUE_MATCHING_DESIGN.md` 참고 —
   DDL 스케치, 리그 배정, 실유저끼리 붙을 때의 비동기 정산(트리거한 쪽은 세이브,
   상대는 `public_club_squads` 공개 스쿼드로 판정), 보상 멱등키, 승격·강등,
   기존 로컬 시즌 롤아웃까지 정리했습니다. 문서에 "판단 필요"로 표시해 둔 세 곳
   (AI 채우기 전 대기시간, 공개 스쿼드 없는 유저 처리, 서버 없는 빌드의 폴백
   존치 여부)은 구현 착수 전 결정이 필요합니다.
4. **실시간 경기 진행 + PvP 개입 보상** — 2절에 이미 있던 현실 시간대 배정·개입
   보상 계획을 3항 위에서 완성합니다.
5. **개인 타이틀 v1 (원작형)** — 리그 전체 실유저 선수 대상 득점왕/도움왕/
   베스트일레븐 + 시즌 보상. 3항(서버 확정 경기 결과) 위에서만 의미가 있습니다.
   어시스트·개인 평점을 매치엔진에 먼저 추가해야 합니다(`playShot()` 안,
   `assistUids` 필드 신설). 데이터는 서버 `player_match_stats` / `season_awards`
   테이블. 유저 소속 선수만 공식 타이틀 후보로 제한(AI 팀은 제외).
6. **카드팩 확률/등급 사다리 확정, 조각 경제 재조정** — 테스트 기간 종료 후로
   미룹니다. 지금 일반팩 골드 10%, 이벤트팩 33%라 테스터가 "주작"이라고 했지만,
   최종 수치 반영은 멀티플레이 루프 검증 이후입니다. 등급 사다리(World → 레전드)
   정리는 이미 코드에 반영됨(`lib/rarity.ts`).
7. 리그컵(상/하반기, 홈앤드어웨이, 통합컵)·앨범/TP/서브팀컬러·팀컬러·케미 확장 —
   3~5항 위에서 순서대로.
8. 보관함 기본 용량 60 → 80 (제안만 된 상태).
9. 최상위 등급 카드 능력치 손보정 — "개좆두드리블이 왜 메시보다 높죠" 지적.
   `lib/rosterOverrides.ts`와 운영자 **선수편집** 탭으로 배포 없이 값을 뽑아냅니다.
10. 프로덕션에서만 나는 React hydration 경고(#425). 내용은 일치하고 React가 스스로
    복구하지만 원인은 아직 못 찾았습니다.

### Codex에 넘길 때의 예시

2·3항은 변수가 서로 물려 있어(뽑기 확률 ↔ 방출 조각 ↔ 교환 비용 ↔ 골드) Codex에
맞는 일입니다. 넘길 때는 이렇게 좁힙니다 — 읽을 파일을 `lib/gacha.ts`,
`lib/shards.ts`, `lib/rarity.ts`, `lib/items.ts` 넷으로 한정하고, 현재 수치를 프롬프트에
직접 적어 주고(다시 찾지 않게), 코드 수정 금지를 명시하고, 산출물 형태를 지정합니다.
"구조를 파악하고 제안해 달라"처럼 범위가 빈 요청을 하면 저장소를 처음부터 훑은 뒤
CLAUDE.md에 적힌 규칙을 되읽어 줍니다 — 실제로 첫 연결 테스트가 그랬습니다.
